apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-failed.fullname" . }}-test-connection"
  labels:
    {{- include "chart-failed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-failed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
